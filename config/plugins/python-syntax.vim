" # Description
  " File storing configuration options and method for the plugin `python-syntax`.

" ## Configuration
  " Set of configuration options for the plugin. I only set variable which
  " differ from the default configuration.

  " Set variable to `1` to enable or `0` to disable.
  "
  " For example to enable all syntax highlighting features you can add the
  " following command to your `~/.config/nvim/init.vim` or `~/.vimrc`:
let g:python_highlight_all = 1
